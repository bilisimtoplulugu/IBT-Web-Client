const initialState = [];
/* const initialState = [{a: 1}, {b: 2}]; */

export const userReducer = (state, action) => {
  const {type, payload} = action;
  switch (type) {
    default:
      return state;
  }
};
